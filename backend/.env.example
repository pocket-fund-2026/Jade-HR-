SUPABASE_URL=
SUPABASE_SERVICE_KEY=
JWT_SECRET=
ALLOWED_ORIGINS=http://localhost:5173
BIOMETRIC_SYNC_SECRET=
# Set to false only for local dev (http, not https) so the session cookie
# will actually be stored by the browser. Never set false in production.
COOKIE_SECURE=false
